org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# --- Maven Central publishing (never commit secrets; set as env or user gradle.properties) ---
# mavenCentralUsername=  # Portal User Token username
# mavenCentralPassword=  # Portal User Token password
# SIGNING_KEY=           # ASCII-armored GPG PRIVATE key
# SIGNING_PASSWORD=      # GPG key passphrase
